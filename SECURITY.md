# Security policy

## 当前范围

`ai-video-app` 当前是本地/单工作空间验收版本，不应直接作为公网生产服务部署。演示账号、固定演示数据和 JSON Store 都不是生产安全配置。

## 凭据处理

- 不要在 Issue、日志、截图、提交、浏览器端变量或 `VITE_*` 环境变量中放入 API Key、密码、支付资料或用户数据。
- 真实提供商凭据仅允许通过服务端专用写入接口进入配置了 `CONTROL_PLANE_MASTER_KEY` 的加密 Vault；密钥不可回显。
- `server/control-plane/data/`、`.env*`（`.env.example` 除外）、构建日志和本地运行数据已被 Git 忽略，不得手动强制提交。

## 上线前最低门禁

- 删除或禁用演示账号，强制所有运营账号使用独立强密码与最小权限；
- 启用 HTTPS、Secure Cookie、可信 Origin、CSP、速率限制和反向代理边界；
- 换用事务型生产数据库与持久任务队列，完成备份、恢复和多实例验证；
- 对供应商回调、支付回调和下载链接做签名验证、重放防护与幂等处理；
- 完成依赖审计、SAST/DAST、越权测试、敏感信息扫描和全链路审计复核。

## 报告问题

请通过仓库所有者指定的私密渠道报告安全问题，不要在公开 Issue 中披露漏洞细节、凭据或真实用户数据。
