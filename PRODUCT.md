# 星核影枢

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 短视频创作者与普通会员：注册、配置创作资源、批量生成视频、管理任务与素材、查看额度和平台通知。
- 代理运营者：在会员能力之外处理推广、佣金与下级关系。
- 超级管理员：在独立后台中高频处理全局配置、用户风控、邀请码与充值码、AI 聚合平台、合伙人密钥池、套餐、教程公告和审计追踪。

## Product Purpose

星核影枢是一套 AI 短视频批量生产与运营系统。会员端负责创作，独立超级管理端负责平台控制；两端必须通过同一份服务端业务数据协作，使冻结账号、调整额度、发布内容和修改平台策略能真实影响会员端。

## Positioning

产品把批量短视频生产流程、额度策略、模型通道和平台运营放在同一条可追踪的控制链中。管理员看到的不是静态报表，而是可执行、可确认、可审计并能同步到会员端的运营状态。

## Operating Context

- 会员与超级管理员通过不同的 Web 入口使用系统。
- 超级管理员主要在电脑端连续处理异常、审批、配置和内容发布，要求高密度、低认知负担和清晰的操作反馈。
- 当前部署目标是腾讯云 Cloud Studio 的独立端口预览域名；它用于联调与验收，不等同于带 SLA、数据库备份和安全运维的正式生产托管。

## Capabilities and Constraints

- 超级管理端拥有最高权限，覆盖全局配置、邀请码、充值码、套餐、用户与风控、AI 聚合平台、合伙人密钥池、教程公告和审计日志。
- 高风险写操作需要确认，成功与失败都必须返回明确结果并写入审计日志。
- 会员端与管理端共享服务端控制数据，不能依赖彼此浏览器中的 `localStorage`。
- AI、短信、支付、提现和对象存储等外部适配器未配置时必须明确显示“未配置”或“不可用”，不得伪造在线、到账或生成成功。
- 密钥不得从服务端回显明文；界面只显示状态、用途、末四位和更新时间。生产密钥需要由服务端加密密钥仓库承载。
- 当前仓库已有 React、TypeScript、Vite 和本地领域状态；独立管理端应复用业务术语与视觉系统，但拥有独立入口、认证和部署端口。

## Brand Commitments

- 产品名：星核影枢；英文辅助名：STELLAR VIDEO ORCHESTRATOR。
- 视觉必须延续登录页与会员端的暗石黑、靛紫、冰蓝高光和魔法文明控制终端语言。
- 产品页优先效率与状态清晰，不能退化成普通紫色 SaaS 卡片墙，也不能让装饰遮挡信息或操作。
- 既有参考资产：`src/assets/arcane-valley.avif`、登录页与现有工作台实现。

## Evidence on Hand

- 最高有效需求基线：V7.1 核心规范 + V5 非冲突必加项 + 上线行动 V6 的运营/RACI；V4.1、V4.2、千问 V6、V1.5 与补遗作为专项补丁和查漏来源。
- 版本裁决与逐项状态记录在 `docs/VERSION_BASELINE.md` 和 `docs/REQUIREMENTS_TRACEABILITY.md`，原始私有 DOCX 不进入源码仓库。
- 已有可运行会员端、认证页、本地状态机与管理页面原型。
- 已有 Cloud Studio 端口 3000 会员端预览配置。
- 当前没有真实 AI 聚合平台凭据、短信/支付供应商、生产数据库、对象存储或正式运营数据；后续界面不得为这些能力制造成功数据。

## Product Principles

1. 真实状态优先：没有接入就说明没有接入，空数据用可行动的空状态表达。
2. 管理动作闭环：操作前说明影响，操作后同步结果，并可在审计日志中追溯。
3. 双端同源：会员端和管理端通过同一控制平面交换配置与账号状态。
4. 高风险最小暴露：最小权限、密钥不回显、敏感动作二次确认。
5. 精美服务效率：强品牌氛围只用于建立秩序和识别，业务优先级永远清楚。
6. 版本可追溯：较新的专项增补可以补充 V7.1，但不得静默改写其权限、安全、额度和隐私规则。

## Accessibility & Inclusion

- 所有操作使用可聚焦的原生控件或具备完整键盘行为的等价控件。
- 可点击区域至少 44px；状态不能只靠颜色表达。
- 支持系统减少动效、触控设备与 390px 移动视口，但超级管理端的主要工作场景是桌面端。
